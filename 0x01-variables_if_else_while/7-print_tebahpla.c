#include <stdio.h>
/**
 * main-print lower case letters in reverse
 * Return:Always 0 (Success)
 */
int main(void)
{
	int R;

	for (R = 122; R >= 97; P--)
	putchar(R);
	putchar("\n");
	return (0);
}


#include <stdio.h>
/**
 * main-Print all single digit numbers
 * of base 10 starting from 0
 * Return:Always 0 (Success)
 */
int main(void)
{
	int Z;

	for (Z = 0; Z <= 9; Z++)
	printf("%i", Z);
	printf("\n");
	return (0);
}

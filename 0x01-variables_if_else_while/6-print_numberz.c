#include <stdio.h>
/**
 * main-Print single digit numbers of
 * base 10 using putchar
 * Return:Always 0 (Success)
 */
int main(void)
{
	int Y;

	for (Y = 48; Y <= 57; Y++)
	putchar(Y);
	putchar('\n');
	return (0);
}


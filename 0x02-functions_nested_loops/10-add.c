#include "main.h"
/**
 * add-a function that adds two integers
 * and returns the result
 * @Q: the first integer to be added
 * @R: the second integer to be added
 * Return:Always 0 (Success)
 */
int add(int Q, int R)
{
	S = Q + R;
	return (S);
}
